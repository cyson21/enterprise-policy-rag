# Decisions

- Admin controls live in Knowledge Library instead of a new dashboard route.
- Static demo mode returns deterministic fallback results for update/delete/audit actions.
- Live admin endpoints remain protected by backend auth context; frontend persona gating is presentation only.
- The UI keeps form fields compact and operational rather than adding a marketing-style admin page.

