# Goal

Add Admin UI controls to the Knowledge Library so the existing admin workflow API is visible and operable from the portfolio demo.

## Scope

- Add document update/delete controls for admin personas.
- Add audit log loading and display.
- Keep non-admin users in a read-only state.
- Keep static public demo working without backend calls.
- Do not add a separate full admin dashboard route.

