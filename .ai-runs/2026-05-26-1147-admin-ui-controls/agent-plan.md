# Agent Plan

1. Add failing frontend smoke labels for admin controls and admin API client functions.
2. Add admin API client helpers with static fallback behavior.
3. Extend Knowledge Library to show admin controls for `activePersona.role === "admin"`.
4. Show audit logs and a read-only notice for non-admin personas.
5. Update docs/TODO/project tracking and run ledger.
6. Run pytest, frontend smoke/build/static smoke, and git checks.
7. Commit, push, and verify Vercel Git deployment.

