# Agent Plan

1. Add failing tests for OIDC JWT session mapping, session-bound retrieval, and invalid token rejection.
2. Implement `OIDCJWTAuthContextProvider` behind the existing provider interface.
3. Add minimal dependency metadata for JWT verification.
4. Update auth design/API docs, project tracking, TODO, bootstrap, and portfolio docs.
5. Run focused auth tests, full pytest, compile, frontend static smoke, and git checks.
6. Commit, push, and verify Vercel Git deployment.

