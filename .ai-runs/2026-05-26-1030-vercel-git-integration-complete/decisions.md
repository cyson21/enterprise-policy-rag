# Decisions

- Use terminal-only Vercel CLI/API checks after the user completed GitHub App authorization.
- Do not reopen visible Chrome while the user is working unless a new manual approval is unavoidable.
- Keep the static public demo deployment as-is; this task only connects Git automation.

