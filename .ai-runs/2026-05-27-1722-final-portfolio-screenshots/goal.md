# Goal

Refresh the final portfolio screenshot bundle for Enterprise Policy RAG after the latest feature set, using the parent side-project screenshot guide as the source of truth.

## Scope

- Capture separate desktop, mobile webview, and mobile full-page screenshots for the Operations route.
- Capture one desktop Knowledge Library admin screenshot so the admin workflow is visible in the portfolio package.
- Use the static fake-provider demo build so screenshots and validation require no API key, Docker, or backend process.
- Inspect the actual generated image files for right/bottom clipping, mobile flow, text fit, and bottom coverage.
- Update README, portfolio, runbook, tracking, and bootstrap docs to point at the refreshed assets.

## Out of Scope

- No OpenAI live call.
- No Docker or PostgreSQL startup.
- No production auth provider setup.
- No visible browser automation; capture should run headlessly.
