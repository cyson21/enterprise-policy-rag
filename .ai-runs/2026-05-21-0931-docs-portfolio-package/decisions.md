# Decisions

- Keep documentation source-trackable as Markdown and SVG.
- Document the current default as in-memory + fake providers, with PostgreSQL integration explicitly optional until Docker is running.
- Mark query log persistence and PostgreSQL round-trip as remaining work instead of overstating completion.
- Keep portfolio one-pager free of personal job-search details.
- Use the runbook as the reproducible demo path for future screenshots and packaging.
