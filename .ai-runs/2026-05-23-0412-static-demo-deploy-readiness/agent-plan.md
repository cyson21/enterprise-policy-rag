# Agent Plan

1. Inspect frontend API fallback behavior and deployment scripts.
2. Add `VITE_DEMO_MODE=static` so public demo builds avoid `/api` calls entirely.
3. Add root scripts for static demo build/preview.
4. Add smoke coverage for static demo mode.
5. Add deployment docs/runbook and update README/TODO/project tracking/bootstrap.
6. Run backend, frontend, static build, and preview verification.
7. Record changed files and verification output.
